module.exports =
  UNARY_LOGICAL_OPS: [
    "$not"
  ]

  UNARY_RELATION_OPS: [
    "$null"
  ]

  BINARY_LOGICAL_OPS: [
    "$and"
    "$or"
    "$xor"
  ]

  BINARY_RELATION_OPS: [
    "$eq"
    "$neq"
    "$gt"
    "$lt"
    "$gte"
    "$lte"
    "$like"
  ]